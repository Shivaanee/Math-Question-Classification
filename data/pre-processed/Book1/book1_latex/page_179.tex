

**23.**: Construct an unsymmetric 2 by 2 matrix of rank 1. Copy Figure 3.4 and put one vector in each subspace. Which vectors are orthogonal?
**24.**: Redraw Figure 3.4 for a 3 by 2 matrix of rank \(r=2\). Which subspace is \(\mathbf{Z}\) (zero vector only)? The nullspace part of any vector \(x\) in \(\mathbf{R}^{2}\) is \(x_{n}=\).
**25.**: Construct a matrix with the required property or say why that is impossible.

1. Column space contains \(\left[\begin{smallmatrix}1\\ 2\\ -3\end{smallmatrix}\right]\) and \(\left[\begin{smallmatrix}2\\ -3\\ 5\end{smallmatrix}\right]\), nullspace contains \(\left[\begin{smallmatrix}1\\ 1\\ 1\end{smallmatrix}\right]\).
2. Row space contains \(\left[\begin{smallmatrix}1\\ 2\\ -3\end{smallmatrix}\right]\) and \(\left[\begin{smallmatrix}2\\ -3\\ 5\end{smallmatrix}\right]\), nullspace contains \(\left[\begin{smallmatrix}1\\ 1\\ 1\end{smallmatrix}\right]\).
3. \(Ax=\left[\begin{smallmatrix}1\\ 1\\ 1\end{smallmatrix}\right]\) has a solution and \(A^{\mathrm{T}}\left[\begin{smallmatrix}0\\ 0\\ 0\end{smallmatrix}\right]=\left[\begin{smallmatrix}0\\ 0\\ 0\end{smallmatrix}\right].\)
4. Every row is orthogonal to every column (\(A\) is not the zero matrix).
5. The columns add up to a column of 0s, the rows add to a row of 1s.
**26.**: If \(AB=0\) then the columns of \(B\) are in the \(\underline{\underline{\underline{\underline{\underline{\underline{\phantom{ \underline{\phantom{\underline{\phantom{\underline{\phantom{\underline{\