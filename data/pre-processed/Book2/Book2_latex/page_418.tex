

## Chapter 18 Nonlinear least squares

### 18.1 Nonlinear least squares

The nonlinear least squares problem is solved by the following nonlinear least squares problem:

\[\begin{split}\tilde{f}(x^{(i)})&=\frac{1}{2}\sum_{i=1 }^{n}\tilde{f}(x^{(i)})\tilde{f}(x^{(i)})\\ &=\frac{1}{2}\sum_{i=1}^{n}\tilde{f}(x^{(i)})\tilde{f}(x^{(i)}) \tilde{f}(x^{(i)})\tilde{f}(x^{(i)})\tilde{f}(x^{(i)})\tilde{f}(x^{(i)})\tilde{f }(x^{(i)})\tilde{f}(x^{(i)