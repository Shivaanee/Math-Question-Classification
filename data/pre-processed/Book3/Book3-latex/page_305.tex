Thus \((L_{M})^{*}=L_{M^{*}}\). In the computation above, we twice used the characteristic property of the trace function: \(\operatorname{tr}\,(AB)\,=\,\operatorname{tr}\,(BA)\).

**Example 20**: _Let \(V\) be the space of polynomials over the field of complex numbers, with the inner product_

\[(f|g)\,=\,\int_{0}^{1}f(t)\overline{g(t)}\;dt.\]

_If \(f\) is a polynomial, \(f=\Sigma\;a_{k}x^{k}\), we let \(\tilde{f}=\Sigma\;a_{k}x^{k}\). That is, \(\tilde{f}\) is the polynomial whose associated polynomial function is the complex conjugate of that for \(f\):_

\[\tilde{f}(t)\,=\,\overline{f(t)},\qquad t\;\text 