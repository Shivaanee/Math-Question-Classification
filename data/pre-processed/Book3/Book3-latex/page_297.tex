and equality holds if and only if_

\[\beta=\sum_{k}\frac{\langle\beta|\alpha_{k}\rangle}{||\alpha_{k}||^{2}}\,\alpha_ {k}.\]

Proof.: Let \(\gamma=\sum\limits_{k}\,[(\beta|\alpha_{k})/||\alpha_{k}||^{2}]\,\alpha_{k}\). Then \(\beta=\gamma+\delta\) where \((\gamma|\delta)=0\). Hence

\[||\beta||^{2}=||\gamma||^{2}+||\delta||^{2}.\]

It now suffices to prove that

\[||\gamma||^{2}=\sum\limits_ 